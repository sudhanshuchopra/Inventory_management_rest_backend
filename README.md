# Inventory_management_rest_backend
<p>It is a backend for inventory management system based on djangorestframework</p>

# Setup
```
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install libpq-dev python-dev
pip install -r requirements.txt
python manage.py migrate
```
